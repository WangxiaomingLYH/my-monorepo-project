/**
 * to：去往的目标页
 * from：来自哪个页，刷新返回自身路由
 */

export default defineNuxtRouteMiddleware((to, from) => {
    // if (import.meta.client) {
    //     console.log(navigator.languages, "@navigator")
    // }
})